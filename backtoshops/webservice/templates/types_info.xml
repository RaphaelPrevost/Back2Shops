<type version="1.0" pk="{{ object.pk }}">
	<name>{{ object.name }}</name>
	{% for attribute in object.common_attributes.all %}
	<attribute pk="{{ attribute.pk }}">
		<name>{{ attribute.name }}</name>
	</attribute>
	{% endfor %}
</type>
